apiVersion: v1
kind: Secret
metadata:
  name: besend-secrets
  namespace: besend
type: Opaque
stringData:
  # Generate a random JWT secret:
  # openssl rand -base64 32
  jwt-secret: "CHANGE_ME_TO_RANDOM_SECRET"  # Replace with: openssl rand -base64 32
